# 작업용 standby 폴더 (TTS 원본, API 키, PDF 등) — 공개 리포에 올리지 않음
halmoni-school_standby/

# Windows 바로가기 파일
*.lnk

# Claude/AI 작업 파일
build_report.js

# 내부 문서는 _config.yml exclude로 웹 차단됨 (리포에는 유지)

tools/__pycache__/
_migrate_nhs_content.mjs

Claude outputs/
doranchae landing pg/

# 딸 UI 자문용 디자인 시안·참고자료 (사이트 코드 아님, doranchae landing pg/와 같은 이유)
_design_handoff/
Halmoni school index redesign.zip
