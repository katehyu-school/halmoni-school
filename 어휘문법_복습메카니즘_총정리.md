# 어휘·문법 복습 메카니즘 총정리
> 2026-08-31 작성 — 도란채(nhs.html)·모바일(dr-mobile.html)·Kids(korean-app_v2.html)에 흩어져 있는
> 모든 "다시 보기/복습" 기능을 한자리에 정리. 새 기능을 추가하기 전엔 이 문서부터 볼 것.

---

## 한눈에 보기

| 기능 | 어디서 | 언제 뜨나 | 콘텐츠 범위 | 저장/지속성 | 다른 기능과 공유? |
|---|---|---|---|---|---|
| 🔥 빠른 복습 (Quick Review) | nhs.html | 편(에피소드)을 열 때 자동 (세션당 1회) | 직전 편들의 어휘 3문제 (뜻 맞추기) | ❌ 없음 — 그 세션 안에서만 "봤음" 기억(`_wuSeen`), 새로고침하면 초기화 | 없음. 완전히 독립적 |
| 🔁 레벨 복습 (Level Review) | nhs.html 사이드바 맨 아래 | 사용자가 직접 클릭 (`loadLevelReview(n)`) | 그 레벨 12편 전체 어휘(최대 30개, cloze+뜻맞추기 혼합) + **문법 SRS 복습**(옛 "문법 모아보기" 요약 탭 자리, 2026-08-31 교체) | 어휘 퀴즈 → 플래시카드 SRS / 문법 카드 → 문법 SRS(`_srs_gram`) | ✅ 플래시카드 SRS 공유 · ✅ 문법은 모바일과 SRS 공유 |
| 🃏 플래시카드 (Flashcard) | nhs.html 사이드바 | 사용자가 직접 클릭 (`openFlashcards()`) | 현재 레벨까지 전체 어휘 풀에서 SRS 규칙대로 최대 25장 자동 구성 | Leitner 상자(1~5) + 복습일, `nms_{프로필}_srs` | ✅ 레벨 복습과 SRS 공유 · ✅ 서버 동기화(`progress_push/pull`) · ✅ 오늘부터 북마크도 이 풀에 합류 |
| 🔖 색인 북마크 | nhs.html 색인(📖) 모달 | 어휘·문법 항목에서 🔖 버튼 클릭 | 사용자가 고른 개별 단어/문법 카드 | Supabase `bookmarks` 테이블 (계정에 귀속, 기기 무관 동기화) | ✅ 어휘 북마크는 플래시카드 SRS 풀에, 문법 북마크는 레벨 복습의 문법 SRS 풀에 자동 합류 |
| 📱 모바일 SRS (단어) | dr-mobile.html | 홈 화면 "오늘의 단어 복습" | 같은 SRS 로직을 모바일에서 재구현 | **웹과 같은 `nms_{프로필}_srs` 저장소** (같은 브라우저면 이어짐) + 서버 push/pull 둘 다(2026-08-31부터, 체험판 유저 이상) | ✅ 웹 플래시카드/레벨복습과 사실상 같은 큐 |
| 📱 모바일 SRS (경어법 문법) | dr-mobile.html | 홈 화면 "오늘의 경어법 복습" | 존댓말/반말 관련 문법만 별도 SRS로 카드화(14개 고정 세트) | `nms_{프로필}_srs_gram` | ✅ 웹 문법 SRS와 **저장 키 공유**(2026-08-31) — 상자·복습일이 기기 간에 합쳐짐. 다만 카드 내용 자체는 서로 다름(모바일=경어법 14개 고정, 웹=그 레벨 전체+북마크) |
| 📓 My Notes | nhs.html + korean-app_v2.html 공용 모듈 | 사용자가 직접 메모 작성 | 학습자가 스스로 적은 자유 메모 | `localStorage` (기기 로컬, My Notes 프로필 이름 기준) | 복습 큐가 아니라 순수 메모장 — SRS와 무관 |
| 📖 색인 (Index, 참고용) | nhs.html | 사용자가 직접 열어서 검색 | 배운 전체 어휘·문법 사전처럼 찾아보기 | 저장 없음(그때그때 조회) | 북마크 버튼의 출발점 |

---

## 1. 🔥 빠른 복습 (Quick Review / Warm-up)
- **함수**: `loadEp()` → `_wuSeen`에 없는 편이면 `_showWarmup()` 호출.
- **문제 출처**: L1은 정적 `WARMUP_POOL`, L2~L6은 `_buildVocabWarmup()`이 **그 레벨 이전 편들의 어휘**(모자라면 이전 레벨까지)로 뜻 맞추기 객관식을 그때그때 자동 생성.
- **성격**: "편 열기 전 준비운동" — 가볍게 3문제만 풀고 넘어감. 점수·기록이 어디에도 저장되지 않는 **완전히 휘발성**인 기능. 같은 세션에서 같은 편을 다시 열면 또 안 뜸(`_wuSeen`), 새로고침하면 다시 뜸.
- **다른 기능과의 관계**: 없음. SRS·서버·북마크 어느 것과도 연결 안 됨.

## 2. 🔁 레벨 복습 (Level Review)
- **함수**: `loadLevelReview(n)` (사이드바 맨 아래 "Level n 복습").
- **어휘 탭**: 그 레벨 **12편 전체**(진도와 무관하게)의 어휘를 모아 최대 30문제 큐 구성.
  - 80%는 고립된 단어 뜻 맞추기(한→영/영→한 번갈아), 20%는 **다른 편에서 그 단어가 실제로 다시 쓰인 진짜 문장**을 찾아 빈칸 채우기(cloze) — "재순환"의 진짜 취지를 살린 방식.
  - 채점 결과는 `_fcMark()`를 통해 **플래시카드와 완전히 같은 SRS 저장소**에 기록됨(주석: "복습 모드와 플래시카드가 같은 기록을 공유합니다").
- **문법 탭** (2026-08-31 교체): 원래는 12편 문법을 편별 한두 줄 요약 + "자세히 보기"로만 보여줬는데, 이게 색인(Index) 문법 탭과 하는 일이 완전히 겹쳐서(둘 다 요약+링크) — 이 자리에 **문법 SRS 복습**을 대신 넣음(`_lvrBuildGramDeck()`). 이 레벨 문법 + 색인에서 북마크한 문법(레벨 무관)을 카드 앞/뒤로 뒤집으며 복습, 결과는 `_srs_gram`에 상자로 기록.
- **다른 기능과의 관계**: 어휘 결과는 플래시카드 SRS와 완전히 같은 큐를 씀 → 레벨 복습에서 틀린 단어가 플래시카드에도 "오늘 복습" 대상으로 뜨고, 반대로 플래시카드에서 맞힌 단어는 레벨 복습에서도 이미 "아는 단어"로 취급됨. 문법 결과는 모바일 경어법 SRS와 저장 키를 공유(진도 동기화로 합쳐짐).

## 3. 🃏 플래시카드 (Flashcard SRS)
- **함수**: `openFlashcards()` (사이드바 "🃏 복습 플래시카드", 옆에 오늘 복습할 개수 배지).
- **카드 풀**: 현재 보고 있는 레벨까지(1~curLevel) 전체 에피소드의 어휘 전부.
- **출제 규칙** (`_fcApplyFilter()`, 최대 25장):
  1. 오늘 복습일이 된 카드 먼저(상자 낮은 것부터 — 자주 틀리는 것 우선)
  2. 자리가 남으면 아직 한 번도 안 본 카드(현재 레벨에 가까울수록 많이 나오게 가중 샘플링)
  3. 그래도 모자라면 복습일 안 된 카드로 채움
- **SRS 규칙**: Leitner 5단계 상자(`_srsUpdate`) — 맞히면 상자+1(간격 1→3→7→16→35일), 틀리면 무조건 상자 1(내일 재시험).
- **저장**: `localStorage`의 `nms_{프로필}_srs`(상자·복습일) + `_fc_review`/`_fc_known`(레거시 호환용). 로그인한 정식 회원이면 3초 디바운스로 Supabase `progress` 테이블에도 자동 백업(`progress_push`), 로그인 시 서버 기록을 로컬과 **합집합으로 병합**(`progress_pull`) — 절대 로컬을 덮어쓰거나 지우지 않음(빈 값으로 서버를 덮어쓰는 것도 방지).
- **오늘 추가된 것**: 🔖 북마크한 어휘는 현재 레벨과 무관하게 이 풀에 항상 합류하고, 처음 북마크한 단어는 "오늘 복습" 큐에 바로 시딩됨(상세는 4번 참고).

## 4. 🔖 색인 북마크 (2026-08-31 신설, 정식 멤버 전용)
- **위치**: 색인(📖) 모달의 어휘·문법 각 행 🔖 버튼 + "즐겨찾기만 보기" 필터.
- **자격**: student/teacher/admin만(서버 RPC `bookmark_toggle`/`bookmark_list`가 `_session_member`로 강제 검증 — trial/guest는 서버에서 거부). 서버 부담 때문에 정식 멤버로 한정.
- **저장**: Supabase `bookmarks` 테이블 — **계정에 귀속**되므로 로그인만 하면 어느 기기에서든 동일하게 보임(localStorage 기반인 SRS/My Notes와 달리 기기 종속이 아님).
- **플래시카드 연동**: `openFlashcards()`가 열릴 때마다 북마크 목록을 불러와, 아직 SRS 풀에 없는 **어휘** 북마크를 전부 추가하고 오늘 날짜로 "복습 예정" 시딩. 이후로는 다른 카드와 똑같이 상자 시스템을 탐. (레벨 필터 걸어도 북마크는 항상 포함됨.)
- **My Notes 탭은 없음**: 처음엔 My Notes에도 북마크 모아보는 탭을 만들었으나 로딩 버그가 있어 선생님 판단으로 되돌림 — **색인 안에서만** 확인 가능.
- **문법 북마크 연동** (2026-08-31 해결): 레벨 복습의 "📖 문법 모아보기" 탭을 열 때마다 북마크 목록을 불러와, 아직 문법 SRS 풀에 없는 문법 북마크를 레벨과 무관하게 전부 추가하고 오늘 날짜로 "복습 예정" 시딩(어휘 북마크→플래시카드와 같은 방식). 카드 id를 북마크 키(`에피소드::id`)와 똑같이 맞춰서 자연스럽게 연결됨.

## 5. 📱 모바일 앱 (dr-mobile.html)
- 웹과 **완전히 별도의 코드**로 같은 개념을 재구현한 구조(공유 모듈이 아니라 복붙+수정).
- **단어 SRS**: 로직·저장 키(`nms_{프로필}_srs`) 전부 웹과 동일 → **같은 브라우저**로 웹/모바일을 오가면 진도가 자연스럽게 이어짐. 로그인 상태면 `progress_push`로 서버에도 올라감.
  - ✅ **해결(2026-08-31)**: `_progPullNow()`를 모바일에도 추가 — 이제 앱을 열면 서버 진도를 먼저 받아 로컬과 합침(웹과 동일 규칙). 체험판 유저 이상 전부 적용(`_progUser()`가 게스트만 제외하도록 수정).
- **경어법 문법 SRS**: `nms_{프로필}_srs_gram` 저장소로, 존댓말/반말 관련 문법 14개만 모아 카드처럼 복습시킴. 웹의 문법 SRS(레벨 복습 "문법 모아보기" 탭)와 **저장 키가 같아서** 진도 동기화로 상자·복습일이 기기 간에 합쳐짐 — 다만 다루는 카드 내용 자체는 서로 다름(모바일=경어법 14개 고정 세트, 웹=그 레벨 전체+북마크).
- **온보딩 문구**: "Practice(맥락 퀴즈) → Review(단어 카드 SRS)" 2단계로 안내.

## 6. 📓 My Notes / 색인 — 복습 큐는 아니지만 관련 기능
- **My Notes**: 학습자가 직접 적는 자유 메모(문법 노트, 궁금한 것 등) + 한글 쓰기 연습 + 꾸미기(아바타) + 뱃지. **SRS나 자동 복습과 무관** — 순수 개인 기록장. `localStorage` 기반, 로그인 없이도 프로필 이름만으로 씀.
- **색인**: 사전처럼 전체 어휘·문법을 검색해서 찾아보는 참고 도구. 그 자체로는 아무것도 저장하지 않지만, 오늘부터 여기서 🔖를 눌러 북마크로 이어짐.

---

## 데이터가 흐르는 방식 (요약)

```
[레벨 복습 어휘] ─┐
                  ├─▶  단어 SRS (nms_{프로필}_srs, Leitner 상자)  ◀──┐
[플래시카드]     ─┘                    │                             │
                                        ▼                             │
                         Supabase progress 테이블  ◀──────────────────┤ push+pull
                         (정식 회원만, 양방향 병합·절대 덮어쓰지 않음)  │  (2026-08-31부터
                                        ▲                              │   모바일도 양방향)
                                        │ push+pull                    │
                            [모바일 앱 단어 SRS] ──────────────────────┘

[레벨 복습 문법] ─┐
                  ├─▶  문법 SRS (nms_{프로필}_srs_gram, Leitner 상자)  ◀── [모바일 경어법 SRS]
                          ▲                                                (2026-08-31부터 같은 키로 동기화)
                          │
              [🔖 색인 북마크] ──▶ Supabase bookmarks 테이블(계정 귀속, 기기 무관)
                          │
                          └─▶ 어휘 북마크는 플래시카드로, 문법 북마크는 레벨 복습 문법 SRS로 자동 합류

[빠른 복습]    독립 — 세션 안에서만, 저장 없음
[My Notes]     독립 — 개인 메모, SRS와 무관
```

---

## 알려진 한계 / 다음에 고려할 것
1. ~~모바일 앱이 서버 진도를 pull 안 함~~ → **해결(2026-08-31)** — `_progPullNow()` 추가, 체험판 유저 이상 전부 적용.
2. ~~문법에는 아직 SRS/플래시카드 개념이 웹에 없음~~ → **해결(2026-08-31)** — 모바일 경어법 SRS를 웹 문법으로 확장, 레벨 복습의 "문법 모아보기" 탭 자리에 넣음(그 탭이 색인 문법 탭과 완전히 중복이라 대체). 색인 문법 북마크가 자동으로 이 큐에 들어오고, 저장 키가 모바일과 같아 진도 동기화로 기기 간에도 합쳐짐.
3. **빠른 복습은 완전히 휘발성** — 세션마다 초기화되고 SRS에 기록이 안 남음. 의도된 설계(가벼운 준비운동)인지, 여기도 SRS에 살짝 반영하면 좋을지는 선생님 판단 필요.
